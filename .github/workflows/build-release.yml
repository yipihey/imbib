name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.2.1)'
        required: false

jobs:
  build-macos:
    name: Build macOS App
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        working-directory: imbib
        run: xcodegen generate

      - name: Import signing certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.MACOS_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create temporary keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Import certificate
          echo "$CERTIFICATE_BASE64" | base64 --decode > $RUNNER_TEMP/certificate.p12
          security import $RUNNER_TEMP/certificate.p12 -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

      - name: Build app
        working-directory: imbib
        run: |
          # Build without code signing first
          xcodebuild -scheme imbib \
            -configuration Release \
            -derivedDataPath $RUNNER_TEMP/DerivedData \
            -destination 'generic/platform=macOS' \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Sign app and extensions
        env:
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          APP_PATH="$RUNNER_TEMP/DerivedData/Build/Products/Release/imbib.app"

          # Find the signing identity
          IDENTITY=$(security find-identity -v -p codesigning | grep "Developer ID Application" | head -1 | awk -F'"' '{print $2}')
          echo "Using identity: $IDENTITY"

          # Sign frameworks first (if any)
          if [ -d "$APP_PATH/Contents/Frameworks" ]; then
            find "$APP_PATH/Contents/Frameworks" -name "*.framework" -o -name "*.dylib" | while read framework; do
              codesign --force --options runtime --sign "$IDENTITY" "$framework"
            done
          fi

          # Sign nested bundles (share extension, safari extension)
          find "$APP_PATH/Contents/PlugIns" -name "*.appex" | while read appex; do
            codesign --force --options runtime --sign "$IDENTITY" --entitlements "$(dirname "$appex")/../../../imbib/imbibSafariExtension/imbibSafariExtension.entitlements" "$appex" 2>/dev/null || \
            codesign --force --options runtime --sign "$IDENTITY" "$appex"
          done

          # Sign the main app
          codesign --force --options runtime --sign "$IDENTITY" "$APP_PATH"

          # Verify
          codesign -dv --verbose=2 "$APP_PATH"

          # Move to export location
          mkdir -p $RUNNER_TEMP/export
          cp -R "$APP_PATH" $RUNNER_TEMP/export/

      - name: Notarize app
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          # Create ZIP for notarization
          cd $RUNNER_TEMP/export
          ditto -c -k --keepParent imbib.app imbib-notarize.zip

          # Submit for notarization
          xcrun notarytool submit imbib-notarize.zip \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --team-id "$TEAM_ID" \
            --wait

          # Staple the notarization ticket
          xcrun stapler staple imbib.app

      - name: Create DMG
        run: |
          # Install create-dmg
          brew install create-dmg

          # Create DMG
          cd $RUNNER_TEMP
          create-dmg \
            --volname "imbib" \
            --volicon "export/imbib.app/Contents/Resources/AppIcon.icns" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "imbib.app" 150 190 \
            --hide-extension "imbib.app" \
            --app-drop-link 450 190 \
            --no-internet-enable \
            "imbib.dmg" \
            "export/imbib.app" || true

          # Fallback to simple DMG if create-dmg fails
          if [ ! -f "imbib.dmg" ]; then
            hdiutil create -volname "imbib" -srcfolder "export/imbib.app" -ov -format UDZO "imbib.dmg"
          fi

      - name: Notarize DMG
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cd $RUNNER_TEMP

          # Submit DMG for notarization
          xcrun notarytool submit imbib.dmg \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --team-id "$TEAM_ID" \
            --wait

          # Staple the DMG
          xcrun stapler staple imbib.dmg

      - name: Get version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Rename DMG with version
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mv $RUNNER_TEMP/imbib.dmg $RUNNER_TEMP/imbib-${VERSION}-macOS.dmg

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: imbib-macos-dmg
          path: ${{ runner.temp }}/imbib-*-macOS.dmg

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ runner.temp }}/imbib-*-macOS.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cleanup keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true
